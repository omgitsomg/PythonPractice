# This is for me to see/test what happens between operations.

# When concatenating two lists together, if one of the lists is empty, then the result will only be the non-empty list.

emptyList = []
numList2 = [6, 7, 8, 9]
numList = [1,2,3,4,5]
print(numList + numList2)

